credentials:
    system:
        domainCredentials:
            - credentials:
                  # Credentials are assumed through the instance profile
                  - aws:
                        scope: GLOBAL
                        description: aws ssimwave deploy
                        id: ssimwave_deploy_dev_aws_credentials
                        accessKey: ""
                        secretKey: ""
                        iamRoleArn: "arn:aws:iam::188664039510:role/ssimwave-dev-ec2-deploy-us-west-2-role"
